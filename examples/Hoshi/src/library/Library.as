package library
{
	import flash.display.Sprite;

	public class Library extends Sprite
	{
		static public var classesNames:Array = new Array(Attacks.getName(), Back.getName(), Chars.getName(), Enemies.getName(), Images.getName());
	}

}